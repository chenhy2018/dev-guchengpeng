TARGETS={ account api/io api/io2 api/fs api/rs api/express api/account api/mi api/mail api/report api/cf admin_api/account admin_api/fs io/sstore iotest iotest2 fstest mockacc mockfs mockrs fileop mockio mi mockqbox mocklogger account/logh auditlog }

all:
	@bmake ${TARGETS}

install:
	@bmake ${TARGETS} install

test:
	@bmake ${TARGETS} test

clean:
	@bmake ${TARGETS} clean
