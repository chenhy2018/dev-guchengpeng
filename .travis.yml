language: go
go:
  - 1.10.1
script:
  - source env.sh
  - cd $TRAVIS_BUILD_DIR/link/vod
  - echo $GOPATH
  - make
  - if [[ "$TRAVIS_BRANCH" == "dev-merge" ]]; then curl https://aone.qiniu.io/api/coverage/collect?token=A59B8029-E9A1-4FF3-B72B-3657CEAC64D4 | bash; else make test; fi
  - make style
